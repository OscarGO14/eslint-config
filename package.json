{
  "name": "eslint-config",
  "version": "1.0.0",
  "description": "proyecto to test eslint config for js",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "http-server ./public .index.js -o /index.html -p 1234",
    "lint": "eslint ./**/*.js",
    "lint:fix": "npx prettier ./**/*.js --write && npm run lint --fix",
    "prepare": "husky install"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^8.36.0",
    "eslint-config-airbnb-base": "^15.0.0",
    "eslint-plugin-import": "^2.27.5",
    "http-server": "^14.1.1",
    "husky": "^8.0.3",
    "prettier": "^2.8.4"
  }
}
